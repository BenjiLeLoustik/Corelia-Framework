<!DOCTYPE html>
<html lang="fr">

    <head>
        <meta charset="UTF-8" />
        <title>Espace de travail {{ name }} créé !</title>
        <link rel="stylesheet" href="/assets/css/default.css" />
        <script>
            function setTheme(theme) {
                document.documentElement.setAttribute('data-theme', theme);
                localStorage.setItem('theme', theme);
            }
            function toggleTheme() {
                const current = document.documentElement.getAttribute('data-theme') || 'light';
                setTheme(current === 'light' ? 'dark' : 'light');
            }
            (function () {
                const saved = localStorage.getItem('theme');
                if (saved) setTheme(saved);
                else if (window.matchMedia("(prefers-color-scheme: dark)").matches) setTheme("dark");
            })();
        </script>
        <style>
            :root {
                --primary: #3e3f44;
                --accent: #00bfae;
                --bg: #f8f9fb;
                --header-bg: #23272f;
                --header-text: #fff;
                --card-bg: #fff;
                --card-shadow: 0 2px 16px rgba(60, 60, 90, 0.06);
                --radius: 14px;
                --font-main: 'Segoe UI', 'Inter', Arial, sans-serif;
            }

            [data-theme="dark"] {
                --primary: #e6e6e6;
                --accent: #00ffd0;
                --bg: #181a1b;
                --header-bg: #181a1b;
                --header-text: #fff;
                --card-bg: #22242a;
                --card-shadow: 0 2px 16px rgba(0, 0, 0, 0.18);
            }

            body {
                margin: 0;
                background: var(--bg);
                color: var(--primary);
                font-family: var(--font-main);
                font-size: 1.1rem;
                min-height: 100vh;
            }

            /* NAVBAR DESIGN */
            .header {
                background: var(--header-bg);
                color: var(--header-text);
                display: flex;
                align-items: center;
                justify-content: space-between;
                padding: 0 44px;
                height: 70px;
                box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
                position: sticky;
                top: 0;
                z-index: 100;
            }

            .logo {
                font-size: 2.1rem;
                font-weight: 700;
                letter-spacing: 1px;
                display: flex;
                align-items: center;
                gap: 12px;
                color: var(--accent);
            }

            .nav {
                display: flex;
                gap: 28px;
            }

            .nav a {
                color: var(--header-text);
                text-decoration: none;
                font-weight: 500;
                font-size: 1.08rem;
                padding: 8px 18px;
                border-radius: 8px;
                transition: background 0.18s, color 0.18s;
                display: flex;
                align-items: center;
                gap: 6px;
            }

            .nav a:hover {
                background: var(--primary);
                color: var(--bg);
            }

            .nav a.active {
                background: var(--accent);
                color: #23272f;
            }

            .theme-toggle-btn {
                background: none;
                border: none;
                font-size: 1.5rem;
                color: var(--accent);
                margin-left: 18px;
                cursor: pointer;
                border-radius: 50%;
                padding: 7px;
                transition: background 0.18s;
            }

            .theme-toggle-btn:hover {
                background: var(--accent);
                color: #23272f;
            }

            /* HERO */
            .hero {
                margin: 0 auto;
                max-width: 900px;
                padding: 60px 24px 34px 24px;
                text-align: center;
                background: linear-gradient(90deg, #00bfae10 0%, #3e3f4410 100%);
                border-radius: var(--radius);
                margin-top: 36px;
            }

            .hero h1 {
                font-size: 2.8rem;
                margin-bottom: 18px;
                font-weight: 800;
                color: var(--primary);
                letter-spacing: 1px;
            }

            .hero h1 span {
                color: var(--accent);
            }

            .hero p {
                font-size: 1.3rem;
                color: var(--primary);
                opacity: 0.85;
                margin-bottom: 32px;
            }

            .hero .cta {
                background: var(--accent);
                color: #23272f;
                border: none;
                font-size: 1.15rem;
                font-weight: 700;
                padding: 16px 38px;
                border-radius: var(--radius);
                cursor: pointer;
                box-shadow: 0 2px 12px #00bfae22;
                transition: background 0.18s, color 0.18s;
                text-decoration: none;
            }

            .hero .cta:hover {
                background: #00cfc0;
                color: #23272f;
            }

            .docs {
                max-width: 1100px;
                margin: 48px auto 0 auto;
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
                gap: 36px;
                padding: 0 24px 48px 24px;
            }

            .card {
                background: var(--card-bg);
                border-radius: var(--radius);
                box-shadow: var(--card-shadow);
                padding: 34px 28px 28px 28px;
                display: flex;
                flex-direction: column;
                gap: 16px;
                transition: transform 0.18s, box-shadow 0.18s;
                cursor: pointer;
            }

            .card:hover {
                transform: translateY(-6px) scale(1.03);
                box-shadow: 0 8px 32px #00bfae22;
            }

            .card .icon {
                font-size: 2.2rem;
                margin-bottom: 10px;
                color: var(--accent);
            }

            .card h2 {
                font-size: 1.35rem;
                margin: 0 0 10px 0;
                font-weight: 700;
            }

            .card p {
                font-size: 1.05rem;
                margin: 0;
                opacity: 0.93;
            }

            code {
                background: #e0f7fa;
                color: #00bfae;
                padding: 2px 8px;
                border-radius: 6px;
                font-size: 1.01rem;
            }

            @media (max-width: 700px) {
                .header {
                    padding: 0 10px;
                }

                .hero {
                    padding: 30px 8px 24px 8px;
                }

                .docs {
                    padding: 0 4px 32px 4px;
                    gap: 18px;
                }
            }
        </style>
    </head>

    <body>

        <header class="header">
            <span class="logo">🚀 CoreliaPHP</span>
            <nav class="nav">
                <a href="{{ APP.BASE }}"><span>🏠</span>CoreliaPHP</a>
                <a href="{{ APP.BASE }}/admin"><span>🛠️</span>Admin</a>
                <a href="{{ APP.BASE }}/mes-sites"><span>🌍</span>Mes sites</a>
                <a href="{{ APP.BASE }}/modules"><span>📦</span>Modules</a>
                <a href="{{ APP.BASE }}/contact"><span>📬</span>Contact</a>
                <button class="theme-toggle-btn" onclick="toggleTheme()" title="Changer de thème">🌓</button>
            </nav>
        </header>

        <section class="hero">
            <h1>🎉 Espace de travail <span>{{ name }}</span> créé&nbsp;!</h1>
            <p>
                Cet espace de travail Corelia est prêt à l’emploi.<br>
                <b>Développez, testez et personnalisez votre application en toute liberté.</b>
            </p>
        </section>

        <section class="docs">

            <div class="card">
                <div class="icon">🚀</div>
                <h2>Démarrer le serveur</h2>
                <p>
                    <code>php corelia serve {{ name }}</code>
                    <br>
                    (Port attribué : <b>{{ port }}</b>)
                </p>
            </div>

            <div class="card">
                <div class="icon">🗂️</div>
                <h2>Structure de l’espace de travail</h2>
                <p>
                    <code>src/Controllers/</code> : vos contrôleurs<br>
                    <code>templates/</code> : vos vues<br>
                    <code>public/</code> : vos assets et index<br>
                    <code>config.json</code> : configuration de l’espace de travail
                </p>
            </div>

            <div class="card">
                <div class="icon">💡</div>
                <h2>Personnalisez&nbsp;!</h2>
                <p>
                    Modifiez ce template dans <code>templates/Welcome/index.ctpl</code><br>
                    Ajoutez vos routes, contrôleurs et vues pour démarrer votre projet.
                </p>
            </div>

            <div class="card">
                <div class="icon">📚</div>
                <h2>Documentation</h2>
                <p>
                    Consultez la doc Corelia pour découvrir toutes les possibilités.<br>
                    <a href="/docs" class="cta" style="margin-top:8px;">Voir la documentation</a>
                </p>
            </div>

        </section>
        
    </body>

</html>